[package]
name = "interop-file"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
interop-contract = { path = "../interop-contract" }
# SHA-256（完整性校验）：RustCrypto 成熟原语，不重写（docs/00 §3.3）。
# 新依赖记录：SEC-07 intake 见 evidence/2026-09-15-t08-storage run manifest。
sha2 = "0.10"

[dev-dependencies]
