{
  "schema_version": 1,
  "profile_id": "dlna.upnp-av",
  "created_at": "2026-09-15",
  "scope": "T41 headless 切片的自制语料计划：SSDP 报文、SOAP 控制信封、设备描述 XML。全部自制；不含抓包或第三方字节",
  "fixture_rules": [
    "每个 fixture 注明它验证字段表的哪一行（fact_ref）与期望结果",
    "负向必须写明期望错误码（本仓错误码或字段表里的 701/402/720）",
    "真机语料后置、由用户执行；pcap 不入库",
    "XML 预算类 fixture 的期望值引用本仓策略常量，不冒充协议常量"
  ],
  "fixtures": [
    { "id": "dl-001", "kind": "positive", "layer": "ssdp", "fact_ref": "F-02",
      "description": "M-SEARCH：`MAN: \"ssdp:discover\"` + MX=2 + ST=urn:…:device:MediaRenderer:1",
      "expected": "解析成功；method/st/mx/man 全部就绪" },
    { "id": "dl-002", "kind": "negative", "layer": "ssdp", "fact_ref": "F-02",
      "description": "缺 MAN / MAN 值不是 ssdp:discover / MX 超出上限（>5）/ 方法为 GET",
      "expected": "invalid-frame（不回 M-SEARCH 之外的语义）" },
    { "id": "dl-003", "kind": "positive", "layer": "ssdp", "fact_ref": "F-03,F-04",
      "description": "NOTIFY：NTS=ssdp:alive（带 LOCATION/CACHE-CONTROL/USN）与 NTS=ssdp:byebye",
      "expected": "alive 进入 registry；byebye 移除；USN 相同的重复通告按更新处理（不重复建条）" },
    { "id": "dl-004", "kind": "negative", "layer": "ssdp", "fact_ref": "F-04",
      "description": "超长头（>8KiB）、头数量超限、USN 为空、LOCATION 缺失",
      "expected": "invalid-frame / resource-limit（解析前拒绝）" },
    { "id": "dl-005", "kind": "positive", "layer": "soap", "fact_ref": "F-05,F-06",
      "description": "SetAVTransportURI / Play / Seek / Stop / GetTransportInfo 的 SOAP 信封（自制）",
      "expected": "动作名与参数解析正确；构造的回包可被同一解析器读回" },
    { "id": "dl-006", "kind": "negative", "layer": "soap", "fact_ref": "F-05,F-06",
      "description": "SOAPACTION 服务类型不符（其它服务/未知服务）、动作名未知、必需参数缺失、InstanceID 非法",
      "expected": "unsupported-feature / invalid-frame（不做尽力解析）" },
    { "id": "dl-007", "kind": "negative", "layer": "xml", "fact_ref": "F-12",
      "description": "含 DOCTYPE/ENTITY 的 XML、深度超预算、元素数超预算、属性数超预算、体积超预算、非法标签名",
      "expected": "invalid-frame / resource-limit；预算是本仓策略值" },
    { "id": "dl-008", "kind": "negative", "layer": "dmc", "fact_ref": "F-04,F-12",
      "description": "LOCATION 指向 file://、含 userinfo、指向 127.0.0.1/169.254.169.254/link-local/组播",
      "expected": "fetch policy 拒绝（T41-01），不发起抓取、不建 registry 条目" },
    { "id": "dl-009", "kind": "negative", "layer": "dmc", "fact_ref": "F-09,F-13",
      "description": "renderer 只声明 rtsp 或未知 MIME，请求 http-get 推送",
      "expected": "拒绝并说明只支持已声明的 http-get 协议；**不得回退成镜像**（T41-02）" },
    { "id": "dl-010", "kind": "negative", "layer": "dms", "fact_ref": "F-07,F-08",
      "description": "Browse 的 ObjectID 不在批准目录、RequestedCount 超上限、BrowseFlag 未知",
      "expected": "701 / resource-limit / 402（T41-03）" },
    { "id": "dl-011", "kind": "invariant", "layer": "dmc", "fact_ref": "T41-04",
      "description": "SetAVTransportURI → Play 之后 Stop（以及超时/取消）",
      "expected": "URL lease 立即撤销：撤销后同一入口不可再取（T41-04）" },
    { "id": "dl-012", "kind": "invariant", "layer": "gate", "fact_ref": "F-13,F-14",
      "description": "任何地方尝试断言品牌/镜像能力",
      "expected": "字段表里 F-13/F-14 保持 impl_allowed=no；实现里不得出现品牌名或 mirror 声明" }
  ],
  "device_corpus": {
    "status": "blocked",
    "blocked_by": "P-M07-1（真实 TV protocolInfo/时序矩阵）",
    "note": "TV 真机抓包与拉流由用户执行；脱敏摘录才进 evidence/dlna/device/"
  }
}
